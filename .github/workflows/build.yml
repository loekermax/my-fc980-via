name: Build QMK VIA Firmware

on: [workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Set up QMK Environment
      run: |
        python3 -m pip install qmk
        qmk setup -y -H ./qmk_firmware

    - name: Use Official Command to Create Project and Compile
      run: |
        cd qmk_firmware
        
        # 1. 呼叫官方標準命令初始化專案結構
        qmk new-keyboard -kb fc980_ec -t STM32F401 -u qmk
        
        # 2. 徹底刪除官方生成的預設 info.json，防止它干擾我們
        rm -f keyboards/fc980_ec/info.json
        
        # 3. 現場寫入 C 語言版 rules.mk (強制開啟 VIA 與指定架構)
        cat << 'EOF' > keyboards/fc980_ec/rules.mk
        MCU = STM32F401
        BOOTLOADER = stm32-dfu
        VIA_ENABLE = yes
        LTO_ENABLE = yes
        EOF
        
        # 4. 現場寫入 C 語言版 config.h (盲測用 6x16 矩陣引腳)
        cat << 'EOF' > keyboards/fc980_ec/config.h
        #pragma once
        #define MATRIX_ROWS 6
        #define MATRIX_COLS 16
        #define MATRIX_ROW_PINS { GPIOA_0, GPIOA_1, GPIOA_2, GPIOA_3, GPIOA_4, GPIOA_5 }
        #define MATRIX_COL_PINS { GPIOB_0, GPIOB_1, GPIOB_2, GPIOB_3, GPIOB_4, GPIOB_5, GPIOB_6, GPIOB_7, GPIOB_8, GPIOB_9, GPIOB_10, GPIOB_12, GPIOB_13, GPIOB_14, GPIOB_15, GPIOA_8 }
        #define DIODE_DIRECTION COL2ROW
        EOF
        
        # 5. 現場寫入符合 6x16 基礎矩陣的 keymap.c 檔
        cat << 'EOF' > keyboards/fc980_ec/keymaps/default/keymap.c
        #include QMK_KEYBOARD_H
        const uint16_t PROGMEM keymaps[][MATRIX_ROWS][MATRIX_COLS] = {
            [0] = {
                { KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A },
                { KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A },
                { KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A },
                { KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A },
                { KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A },
                { KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A, KC_A }
            }
        };
        EOF
        
        # 6. 強制進行雲端編譯
        qmk compile -kb fc980_ec -km default

    - name: Upload Finished Firmware
      uses: actions/upload-artifact@v4
      with:
        name: fc980-ec-via-firmware
        path: qmk_firmware/.build/*.bin
